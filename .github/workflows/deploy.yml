- name: 🚀 Deploy amb rsync
  uses: burnett01/rsync-deployments@v1.0.1
  with:
    switches: -avzr --delete
    path: build/
    remote_path: /ruta/al/servidor/
    remote_host: ${{ secrets.SSH_HOST }}
    remote_user: ${{ secrets.SSH_USER }}
    remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
